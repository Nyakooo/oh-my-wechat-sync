# 微信个人增量备份系统：技术架构与可执行开发计划

状态：规划稿 v1

本文是本项目的开发总纲。它把“微信聊天记录同步方案”整理成可落地的产品边界、技术验证闸门、模块设计、开发阶段和验收标准。

> 重要：微信运行时、数据库加密/密钥、消息 Schema 和媒体读取方式尚未在本项目中验证。P0 技术验证完成前，不得把候选方案当成已实现能力，也不得直接开始大规模 Web UI 开发。

---

## 1. 项目定义

### 1.1 产品目标

本项目是一个面向个人和家庭服务器/NAS 的自托管微信聊天记录归档系统：

- 支持多个微信账号；
- 用户通过 Web 管理账号和发起同步；
- 首次扫码登录成功后自动启动全量归档；之后只有用户点击“立即同步”才执行增量归档；
- 通过回看窗口和唯一键实现可靠增量归档；
- 归档数据保存到独立的 SQLite + 本地媒体存储；
- 通过只读 Web UI 浏览、搜索和查看聊天记录；
- 低配置服务器平时只运行轻量的 Web/API，默认不常驻微信 Runtime；
- `/data` 是完整的数据边界，便于迁移和备份。

一句话定义：

> 微信负责产生数据，Archive 负责长期保存；系统只读展示，不替用户发送消息。

### 1.2 已确认的产品决策

| 决策 | 结论 |
|---|---|
| 首次同步触发 | 首次扫码登录成功后自动创建并开始一次全量同步；不再要求用户额外点击“开始首次同步” |
| 后续增量触发 | 首次同步完成后，仅用户手动点击“立即同步”才开始增量同步 |
| 是否定时同步 | V1 不做定时任务；自动触发只用于首次扫码成功和等待扫码任务的恢复 |
| 账号数量 | 多账号；账号数据、Runtime、同步状态相互隔离 |
| 并发策略 | V1 默认同一时间只同步一个账号 |
| 归档策略 | Append-only；微信删除不等于 Archive 删除 |
| Web 能力 | 只读浏览、媒体查看、全文搜索、同步状态 |
| 消息发送 | 不做 |
| 长期运行服务 | 一个轻量 Archive 服务即可 |
| 数据库 | SQLite + WAL + FTS5 |
| 媒体存储 | 本地文件系统，按内容哈希去重 |
| 部署 | Docker Compose 优先，单机部署 |
| 远程访问 | 可通过反向代理提供，但认证和 HTTPS 必须由部署配置覆盖 |

### 1.3 明确不做

V1 不包含：

- 发送消息、自动回复、消息监听；
- 朋友圈、公众号、群机器人或社交能力；
- 多用户协作、云端账号体系、公开注册；
- 定时同步、常驻微信进程、实时推送；
- AI 总结、向量检索、OCR、语音转文字；
- 修改微信原始数据库或删除微信源端数据；
- 把 Web UI 做成可操作的微信替代客户端。

---

## 2. 先解决的现实问题：数据链路可行性

本项目的最大风险不是 UI，而是以下链路能否稳定成立：

```text
微信 Runtime
  -> 登录状态与数据目录
  -> 数据库是否加密
  -> 密钥如何获得
  -> 消息/联系人/会话 Schema
  -> 图片、视频、语音、文件读取
  -> 在不损坏源数据的前提下复制和解析
```

原方案中提到的 Linux 微信 + Xvfb + Docker、WechatOnCloud 思路，以及 Windows Agent，均视为候选实现，不视为已经验证的依赖。

### 2.1 P0 的技术闸门

P0 必须回答并留下可复现证据：

1. 当前目标部署架构是 `x86_64`、`arm64` 还是两者都支持；
2. 候选微信 Runtime 能否在目标环境启动；
3. 是否能通过 Web 看到二维码并完成扫码登录；
4. 登录态是否能持久化，停止后是否能恢复；
5. 微信数据目录实际位置、权限和生命周期；
6. 数据库是否加密，密钥获取是否稳定且不依赖不可接受的人工操作；
7. 是否能只读读取联系人、会话、文本消息；
8. 是否能读取至少一种媒体并关联到消息；
9. 微信版本升级后 Adapter 是否有可维护的隔离点；
10. 在账号和合规风险可接受的前提下，是否适合用真实账号继续测试。

### 2.2 运行时方案决策顺序

按以下顺序验证，不提前绑定业务层：

| 方案 | 说明 | 进入 V1 的条件 |
|---|---|---|
| A：Linux Docker Runtime | Runtime 与 Archive 同机，由 Docker 管理 | P0 在目标架构上完成登录、数据持久化和只读读取 |
| B：外置 Windows Agent | Archive 部署在服务器，采集器部署在可运行微信的 Windows 主机 | A 不可行，但 Agent 能稳定输出版本化数据协议 |
| C：导入已有数据目录 | 暂不负责登录，只导入用户提供的微信数据 | A/B 均未打通时作为离线 MVP |

P0 的退出规则：

- A 验证失败时，不得通过猜测密钥、绕过权限或改写业务层来掩盖失败；
- 如果 B 成为正式路线，必须重新明确 Agent 的部署、网络、认证和故障恢复边界；
- 如果只有 C 可用，它只能作为开发/演示或兼容路径，不能满足当前用户确认的扫码后自动同步产品目标；需要返回与用户确认替代交付边界，不得擅自把产品降级为手动导入系统。

---

## 3. 总体架构

低配置场景下不拆微服务。长期运行只有一个 Archive 服务；微信 Runtime 按账号按需启动，并且由同步锁限制并发。

```text
                         Browser
                            |
                       HTTP / SSE
                            v
┌────────────────────────────────────────────────────┐
│                  Archive Service                    │
│                                                    │
│  Web UI  <->  API  <->  Sync Orchestrator          │
│                         |                          │
│       ┌─────────────────┼─────────────────┐        │
│       v                 v                 v        │
│  Runtime Manager   WeChat Adapter    Archive Store │
│       |                 |             SQLite/FTS5  │
│       v                 v                 |        │
│  微信 Runtime      源数据只读扫描       Media Store │
└───────┼─────────────────┼─────────────────┼────────┘
        v                 v                 v
    账号容器/卷       联系人/消息/媒体       /data
```

### 3.1 建议技术栈

以“易维护、低资源、单机部署”为目标，采用：

```text
Frontend: Vue 3 + TypeScript + Vite
Backend:  FastAPI + Python
Storage:  SQLite + WAL + FTS5 + 本地文件系统
Runtime:  Docker Compose / Docker Engine API（按需创建或启动）
Deploy:   Dockerfile + compose.yaml
```

选择 Python/FastAPI 是因为 P0 的数据库解析和适配器验证更容易快速迭代；选择 Vue 3 是因为 Web UI 规模适中，适合做桌面式双栏阅读器。若 P0 证明解析库或运行环境更适合其他语言，只能在 P0 评审时调整，不能在开发中途无记录切换。

### 3.2 资源原则

平时：

```text
Archive Web/API     运行
SQLite              运行
微信 Runtime A/B/C  停止
同步任务            空闲
```

点击同步时：

```text
获取全局同步锁
  -> 启动目标账号 Runtime
  -> 检查登录态
  -> 等待数据可读
  -> 扫描、解析、归档
  -> 校验并提交 checkpoint
  -> 停止 Runtime
  -> 释放同步锁
```

V1 不引入 Redis、RabbitMQ、Celery、PostgreSQL、Elasticsearch 或 MinIO。

---

## 4. 数据边界与目录设计

所有可迁移数据统一放到 `/data`：

```text
/data/
├── archive.db                    # 归档库
├── media/
│   ├── image/
│   ├── video/
│   ├── voice/
│   ├── file/
│   ├── emoji/
│   └── avatar/
├── accounts/
│   └── <account-id>/
│       ├── runtime/               # 微信 Runtime 持久化数据
│       ├── source-manifest.json   # 源数据识别信息，不含秘密
│       └── logs/
├── jobs/
├── cache/
└── logs/
```

原则：

- 微信源数据与 Archive 数据分开；
- 归档过程只读源数据；
- Archive 删除操作不反向删除微信源数据；
- 媒体文件使用 `sha256` 或等价内容哈希作为物理存储去重键；
- 数据库中的原始字段/原始 XML/原始 JSON 只作为兼容和排错手段，不能替代标准化字段；
- 备份 `/data` 应足以迁移 Archive，但运行时镜像和 Compose 配置仍需单独保存。

---

## 5. 归档数据模型

以下是 V1 的逻辑模型。实际迁移脚本可以按 Adapter 验证结果微调，但不得删除来源标识和去重约束。

### 5.1 账号与同步

```sql
accounts (
  id                 TEXT PRIMARY KEY,
  display_name       TEXT NOT NULL,
  wechat_id          TEXT,
  avatar_path        TEXT,
  runtime_kind       TEXT NOT NULL,
  runtime_ref        TEXT,
  status             TEXT NOT NULL,
  created_at         INTEGER NOT NULL,
  updated_at         INTEGER NOT NULL,
  last_sync_at       INTEGER
)

sync_jobs (
  id                 TEXT PRIMARY KEY,
  account_id         TEXT NOT NULL,
  trigger            TEXT NOT NULL,       -- manual
  status             TEXT NOT NULL,       -- queued/running/waiting_qr/failed/completed/cancelled
  phase              TEXT,
  started_at         INTEGER,
  finished_at        INTEGER,
  error_code         TEXT,
  error_message      TEXT,
  stats_json         TEXT,
  FOREIGN KEY (account_id) REFERENCES accounts(id)
)

sync_checkpoints (
  account_id         TEXT NOT NULL,
  source_name        TEXT NOT NULL,
  source_fingerprint TEXT,
  last_source_id     TEXT,
  last_source_time   INTEGER,
  overlap_size       INTEGER NOT NULL DEFAULT 1000,
  updated_at         INTEGER NOT NULL,
  PRIMARY KEY (account_id, source_name),
  FOREIGN KEY (account_id) REFERENCES accounts(id)
)
```

### 5.2 联系人、会话、消息

```sql
contacts (
  id                 TEXT PRIMARY KEY,
  account_id         TEXT NOT NULL,
  source_contact_id  TEXT NOT NULL,
  display_name       TEXT,
  alias              TEXT,
  avatar_path        TEXT,
  raw_payload        TEXT,
  UNIQUE (account_id, source_contact_id)
)

conversations (
  id                 TEXT PRIMARY KEY,
  account_id         TEXT NOT NULL,
  source_chat_id     TEXT NOT NULL,
  kind               TEXT NOT NULL,       -- direct/group/unknown
  title              TEXT,
  avatar_path        TEXT,
  last_message_at    INTEGER,
  UNIQUE (account_id, source_chat_id)
)

conversation_members (
  conversation_id    TEXT NOT NULL,
  contact_id         TEXT NOT NULL,
  PRIMARY KEY (conversation_id, contact_id)
)

messages (
  id                 TEXT PRIMARY KEY,
  account_id         TEXT NOT NULL,
  conversation_id    TEXT NOT NULL,
  sender_contact_id  TEXT,
  source_msg_id      TEXT NOT NULL,
  source_created_at  INTEGER NOT NULL,
  type               TEXT NOT NULL,
  content            TEXT,
  is_self            INTEGER NOT NULL DEFAULT 0,
  reply_to_id        TEXT,
  raw_payload        TEXT,
  archived_at        INTEGER NOT NULL,
  UNIQUE (account_id, source_msg_id)
)
```

### 5.3 媒体与全文索引

```sql
attachments (
  id                 TEXT PRIMARY KEY,
  account_id         TEXT NOT NULL,
  message_id         TEXT NOT NULL,
  kind               TEXT NOT NULL,
  mime_type          TEXT,
  original_name      TEXT,
  size               INTEGER,
  source_path        TEXT,
  archive_path       TEXT,
  sha256             TEXT,
  status             TEXT NOT NULL,
  UNIQUE (account_id, message_id, kind, sha256)
)

messages_fts USING fts5 (
  message_id UNINDEXED,
  account_id UNINDEXED,
  conversation_id UNINDEXED,
  content,
  tokenize = 'unicode61'
)
```

`messages` 的唯一键必须包含账号维度。不同微信账号即使出现相同的源消息 ID，也不能互相覆盖。

---

## 6. 同步设计

### 6.1 首次添加账号与扫码后自动归档

```text
添加账号
  -> 创建 account 记录
  -> 创建独立 Runtime 数据目录/容器
  -> 启动 Runtime
  -> Web 展示二维码或 Runtime 登录页面
  -> 用户扫码并确认
  -> 读取账号身份并绑定
  -> 登录成功事件触发一次首次全量同步
  -> 展示进度、统计和结果
```

登录和同步仍是两个可观察的阶段，但首次扫码成功会自动排队同步，不要求用户再次点击。必须在归档账号上持久化 `initial_sync_started`/状态，确保 Runtime 重复上报登录成功时不会重复创建首次任务。

### 6.2 后续手动增量同步与扫码恢复

```text
首次同步完成后，用户点击“立即同步”
  -> 检查是否存在 running job
  -> 获取全局锁
  -> 启动目标 Runtime（已运行则复用）
  -> 检查登录态
       ├─ 失效：当前任务进入 waiting_qr，展示二维码；扫码成功后自动恢复当前任务
       └─ 正常：继续增量同步
  -> 等待源数据库稳定
  -> 读取联系人和会话变化
  -> 对每个消息源回看最近 N 条/一段时间
  -> 标准化消息和附件
  -> 使用唯一键 upsert/insert-or-ignore
  -> 复制媒体并按 sha256 去重
  -> 更新 FTS5
  -> 执行计数校验、外键校验、媒体存在性校验
  -> 成功提交 checkpoint
  -> 写入 sync_jobs 统计
  -> 停止 Runtime（除非用户配置为保持运行）
  -> 释放锁
```

### 6.3 增量不可只依赖时间

不能只使用：

```sql
WHERE timestamp > last_sync_time
```

原因包括乱序消息、延迟写入、多个数据库分片和时间精度差异。V1 使用组合策略：

1. 记录源库/源表的文件指纹、大小、修改时间和 WAL 信息；
2. 记录每个消息源的 `last_source_id` 与 `last_source_time`；
3. 每次回看最近 N 条或最近一段时间；
4. 通过 `(account_id, source_msg_id)` 去重；
5. 只有所有分片和媒体处理成功后才推进 checkpoint。

宁可重复扫描，也不要为了少扫数据而漏掉消息。

### 6.4 失败与恢复

- 任意阶段失败，`sync_jobs.status` 必须变为 `failed`，保留错误码、阶段和可读信息；
- checkpoint 只能在批次校验成功后推进；
- 重试必须幂等，不得产生重复消息或重复物理媒体；
- 取消任务时停止后续批次，已提交数据保留；
- Runtime 启动失败、登录失效、数据解密失败、源库锁定和磁盘不足要分别给出用户可理解的错误；
- 无论成功或失败，都要执行 Runtime 清理/停止和锁释放。

### 6.5 Archive 与微信源端的关系

Archive 是追加式归档，不是镜像：

```text
微信源端删除消息  !=  删除 Archive 消息
```

V1 不做源端删除检测和反向删除。若未来增加 Archive 删除，必须是显式、可确认、可审计的操作。

---

## 7. Web 产品设计

### 7.1 首页 Dashboard

每个账号卡片显示：

- 账号昵称/头像；
- 登录状态；
- 上次同步时间；
- 已归档消息数、媒体大小；
- 最近一次同步结果；
- `查看聊天`、`立即同步`、`账号设置`。

首页必须明确区分：

```text
已登录但未同步
正在等待扫码
正在同步
同步完成
同步失败
```

### 7.2 同步进度

不要只显示旋转图标。至少展示：

- 当前阶段：启动 Runtime / 检查登录 / 扫描 / 导入消息 / 归档媒体 / 校验；
- 当前账号；
- 已处理与总量（无法预估时显示已处理）；
- 新增消息、媒体、跳过重复项；
- 最近错误；
- 取消按钮。

进度传输 V1 可使用 SSE；如果 P0/后端框架暂时不支持，先用短轮询完成闭环。

### 7.3 只读聊天查看器

布局：

```text
┌────────────────┬──────────────────────────────────────┐
│ 搜索/会话列表  │ 会话标题                              │
│                │                                      │
│ 张三           │ 时间分隔                              │
│ 工作群         │ 对方消息              我的消息        │
│ 家人群         │ 图片 / 文件 / 视频 / 语音            │
│                │                                      │
└────────────────┴──────────────────────────────────────┘
```

V1 优先保证：

- 会话列表和最后一条消息；
- 消息分页/按时间加载；
- 文本、图片、文件；
- 群聊发送者；
- 日期分隔与消息时间；
- 搜索结果跳转到会话和消息位置。

视频、语音、表情、引用消息可以按 Adapter 能力分阶段接入，但数据模型要预留 `type` 和附件关系。

### 7.4 搜索

搜索范围至少包括：

- 当前账号或全部账号；
- 当前会话或全部会话；
- 日期范围；
- 消息类型；
- 文本内容。

搜索索引必须包含所有已归档消息。隐藏会话、折叠账号或分页加载不能从索引中删除数据。

---

## 8. API 草案

API 以 `/api/v1` 为前缀，错误响应统一包含 `code`、`message`、`details`。

### 账号与 Runtime

```text
GET    /api/v1/accounts
POST   /api/v1/accounts
GET    /api/v1/accounts/{account_id}
PATCH  /api/v1/accounts/{account_id}
DELETE /api/v1/accounts/{account_id}        # 必须二次确认；V1 可延后
POST   /api/v1/accounts/{account_id}/runtime/start
POST   /api/v1/accounts/{account_id}/runtime/stop
GET    /api/v1/accounts/{account_id}/runtime/status
GET    /api/v1/accounts/{account_id}/runtime/qr
```

### 同步

```text
POST   /api/v1/accounts/{account_id}/sync
GET    /api/v1/sync-jobs/{job_id}
GET    /api/v1/sync-jobs/{job_id}/events
POST   /api/v1/sync-jobs/{job_id}/cancel
GET    /api/v1/accounts/{account_id}/sync-history
```

### 归档浏览

```text
GET    /api/v1/accounts/{account_id}/conversations
GET    /api/v1/conversations/{conversation_id}/messages
GET    /api/v1/messages/{message_id}
GET    /api/v1/attachments/{attachment_id}
GET    /api/v1/search?q=...
```

### 系统

```text
GET    /healthz
GET    /api/v1/storage/stats
GET    /api/v1/system/capabilities
```

API 不直接暴露微信源数据库路径，也不允许通过 URL 任意读取文件。

---

## 9. 项目目录建议

```text
.
├── apps/
│   ├── api/
│   │   ├── routes/
│   │   ├── services/
│   │   ├── schemas/
│   │   └── main.py
│   └── web/
│       ├── src/
│       └── vite.config.ts
├── packages/
│   ├── archive-db/
│   ├── sync-engine/
│   ├── runtime-manager/
│   └── wechat-adapters/
├── migrations/
├── tests/
│   ├── fixtures/
│   ├── unit/
│   ├── integration/
│   └── e2e/
├── deploy/
│   ├── Dockerfile
│   ├── compose.yaml
│   └── compose.example.env
├── docs/
└── README.md
```

解析器与归档引擎必须通过稳定的内部接口连接：

```python
class WeChatAdapter(Protocol):
    def detect(self) -> SourceInfo: ...
    def check_login(self) -> LoginState: ...
    def scan_contacts(self) -> Iterable[SourceContact]: ...
    def scan_conversations(self) -> Iterable[SourceConversation]: ...
    def scan_messages(self, scope: ScanScope) -> Iterable[SourceMessage]: ...
    def resolve_attachment(self, message: SourceMessage) -> Iterable[SourceAttachment]: ...
```

归档引擎只依赖标准化源对象，不依赖某个微信版本的表名或文件路径。

---

## 10. 可执行开发阶段

所有阶段都必须满足“交付物 + 退出条件”才算完成。

### Phase 0：技术验证闸门（最高优先级）

目标：确认至少一条可接受的数据读取链路。

任务：

- [ ] 固定测试机器架构、系统、Docker 版本和微信版本；
- [ ] 建立隔离测试账号，不使用最重要的主账号；
- [ ] 记录 Runtime 启动、登录、扫码和登录态持久化证据；
- [ ] 定位源数据目录并确认挂载权限；
- [ ] 确认数据库加密状态和合法/稳定的读取方式；
- [ ] 导出联系人、会话、文本消息最小样本；
- [ ] 导出至少一种媒体并验证消息关联；
- [ ] 重启 Runtime 后重复读取；
- [ ] 模拟源库变化并证明第二次扫描能发现变化；
- [ ] 形成 `docs/p0-feasibility-report.md`，写明选择 A/B/C 和放弃的方案。

交付物：

```text
P0 fixture 数据
P0 adapter prototype
P0 运行记录和截图/日志
可重复执行的验证命令
技术可行性报告
```

退出条件：

- 能稳定获得账号身份；
- 能只读读取联系人、会话和文本消息；
- 能读取并复制至少一种媒体；
- 重复执行不会修改源数据；
- 失败场景可识别；
- 明确 V1 Runtime 路线。

若退出条件不满足，停在 P0，先修正 Runtime/Adapter 方案，不进入 UI 大规模开发。

### Phase 1：基础骨架与归档数据库

目标：在不接真实 Runtime 的情况下，用 fixture 跑通归档链路。

任务：

- [ ] 初始化 FastAPI、Vue 3、测试和迁移工具；
- [ ] 实现 SQLite WAL、迁移、外键和 FTS5 初始化；
- [ ] 建立 accounts、contacts、conversations、messages、attachments、sync_jobs、sync_checkpoints；
- [ ] 实现标准化 DTO 和 Adapter 接口；
- [ ] 准备可提交到仓库的脱敏 fixture；
- [ ] 实现消息唯一键、媒体哈希和幂等写入；
- [ ] 完成数据库单元测试。

退出条件：fixture 首次导入成功，重复导入不增加重复消息/文件，测试可在空目录中重跑。

### Phase 2：扫码后自动首次同步与后续手动增量引擎

目标：完成真实的“扫码成功 -> 自动首次全量归档；之后点击同步 -> 增量归档”流程；不提供定时任务。

任务：

- [ ] 实现全局同步锁和按账号状态机；
- [ ] 实现 Runtime start/stop/status 抽象；
- [ ] 实现 Runtime 登录状态事件、扫码成功自动触发首次全量同步和重复事件幂等；
- [ ] 实现登录失效与 `waiting_qr` 状态，扫码恢复后自动续跑；
- [ ] 实现源文件指纹、回看窗口和 checkpoint；
- [ ] 实现分批导入、事务边界和断点恢复；
- [ ] 实现媒体复制、哈希去重和缺失文件状态；
- [ ] 实现同步统计和结构化错误码；
- [ ] 验证成功/失败/取消/重试/磁盘不足/Runtime 崩溃。

退出条件：同一账号重复同步幂等；两个账号互不覆盖；同步中途退出后可恢复；微信删除不影响已归档记录。

### Phase 3：账号与 Web API

目标：不依赖命令行即可管理账号和同步任务。

任务：

- [ ] 实现账号 CRUD 和 Runtime API；
- [ ] 实现发起同步、查询状态、取消、历史记录；
- [ ] 实现 SSE 或轮询进度；
- [ ] 实现会话列表、消息分页、媒体访问和全文搜索 API；
- [ ] 实现 `/healthz` 和存储统计；
- [ ] 限制文件访问范围并统一错误响应。

退出条件：API OpenAPI 文档可生成；接口覆盖成功、失败、未登录、账号不存在、并发同步等状态。

### Phase 4：只读 Web 客户端

目标：完成日常可用的“像微信一样浏览归档”体验。

任务：

- [ ] Dashboard 账号卡片和同步按钮；
- [ ] 添加账号、二维码/登录态页面；
- [ ] 同步进度和结果页面；
- [ ] 左侧会话列表、右侧消息阅读器；
- [ ] 图片预览、文件下载/元信息、视频/语音占位或播放；
- [ ] 全局搜索、过滤和结果跳转；
- [ ] 移动端至少保持可读，桌面端优先。

退出条件：新用户无需看日志即可完成添加账号、同步、打开会话、搜索文本和查看媒体。

### Phase 5：Docker 与低资源部署

目标：在干净主机上可重复部署和迁移。

任务：

- [ ] 多阶段构建 Archive 镜像；
- [ ] 编写 `compose.yaml`、环境变量示例和数据卷；
- [ ] 增加健康检查和优雅停止；
- [ ] 明确 Docker Socket/Runtime 管理权限；
- [ ] 验证容器重启、数据卷迁移和磁盘满时行为；
- [ ] 编写升级与回滚说明；
- [ ] 若采用外置 Agent，补充 Agent 的网络和认证部署文件。

退出条件：`docker compose up -d` 后能访问 Web；重启不丢数据；复制 `/data` 到新主机可恢复归档；Runtime 不同步时默认不占用持续运行资源。

### Phase 6：V1 稳定化

目标：让系统适合长期个人使用。

任务：

- [ ] 增加数据健康检查、孤儿媒体扫描和 FTS 重建；
- [ ] 增加归档导出/导入或明确数据库迁移策略；
- [ ] 补齐日志脱敏、权限和认证配置；
- [ ] 进行大数据量分页、搜索和媒体存储测试；
- [ ] 为微信版本变化维护 Adapter capability matrix；
- [ ] 编写运维手册和故障排查手册。

V1 仍不添加定时同步；若未来需要，另立需求和架构评审。

---

## 11. 测试与验收矩阵

### 数据完整性

- [ ] 首次导入后消息数量与 fixture/源端统计一致；
- [ ] 同一批次重复导入不产生重复消息；
- [ ] 不同账号的同名联系人、相同源 ID 不互相覆盖；
- [ ] 消息与会话、发送者、附件关系可追溯；
- [ ] 原始数据路径不可通过 API 任意读取。

### 增量可靠性

- [ ] 新消息能被发现；
- [ ] 乱序消息在回看窗口内能被发现；
- [ ] 源库文件变化但无新消息时不会产生脏数据；
- [ ] 中途崩溃后重试不会重复；
- [ ] checkpoint 只在成功校验后推进；
- [ ] 源端删除不会删除 Archive 数据。

### 多账号与资源

- [ ] 账号 A 同步时账号 B 的归档数据不变化；
- [ ] 并发点击只产生一个有效同步任务；
- [ ] Runtime 生命周期符合启动/停止策略；
- [ ] 空闲时没有隐藏的定时同步或常驻微信进程；
- [ ] 磁盘不足能停止并给出可读错误。

### Web 体验

- [ ] 可以添加账号并看到登录/扫码状态；
- [ ] 可以看到同步阶段和结果统计；
- [ ] 可以打开会话并按时间分页；
- [ ] 可以搜索并跳转到消息；
- [ ] 图片/文件访问不绕过账号和路径边界；
- [ ] 移动端不出现无法关闭的横向布局。

### 部署与恢复

- [ ] 全新主机可用 Compose 启动；
- [ ] 容器重启不丢 SQLite、媒体和 Runtime 持久化数据；
- [ ] `/data` 备份后可在另一目录恢复；
- [ ] 健康检查能区分服务存活和归档功能可用；
- [ ] 日志不输出密钥、二维码内容或完整聊天正文。

---

## 12. 安全与隐私要求

聊天记录是高敏感个人数据，必须默认保守：

- 默认监听本机或由部署者显式配置监听地址；
- 远程访问必须配合认证和 HTTPS；
- 不把 Docker Socket 暴露给浏览器；
- Runtime 管理权限限制在后端内部；
- API 禁止任意路径、目录遍历和任意容器操作；
- 日志默认脱敏，不记录消息全文、二维码和登录凭据；
- 不向第三方上传聊天数据；
- 真实账号测试前先使用隔离账号并评估 Runtime/Hook 路线风险；
- 明确源数据和归档数据的删除、备份、迁移责任；
- 任何永久删除操作都必须显式确认，并保留审计记录。

---

## 13. 交付顺序与最小可用版本

### 最小可用版本（MVP）

MVP 只要求：

1. 一条通过 P0 验证的 Runtime/导入链路；
2. 一个账号能完成扫码或数据源绑定；
3. 首次导入联系人、会话、文本消息；
4. 手动点击后完成增量扫描、去重和 checkpoint；
5. SQLite 持久化；
6. Web 查看会话和文本消息；
7. FTS5 搜索；
8. Docker Compose 启动；
9. 源数据只读、Archive append-only。

### V1 增强

- 多账号完整 UI；
- 图片、文件、视频、语音、表情；
- 进度 SSE；
- 搜索过滤和时间跳转；
- 健康检查、统计和运维文档；
- 版本化 Adapter 能力矩阵。

### 后续候选

- 定时同步（必须重新评审资源和通知策略）；
- 更细的归档导出格式；
- 更强的全文检索；
- 受控的多用户只读访问。

---

## 14. 第一批可直接执行的开发任务

以下任务应按顺序执行，每项完成后更新本文件的勾选状态和对应证据链接。

### Task 01：建立 P0 验证记录

- [ ] 创建 `docs/p0-feasibility-report.md`；
- [ ] 记录目标主机、架构、Docker、微信版本；
- [ ] 记录候选 Runtime 的来源、许可证和已知风险；
- [ ] 禁止使用主账号，准备隔离测试账号。

### Task 02：验证 Runtime 生命周期

- [ ] 启动 Runtime；
- [ ] 通过 Web 观察二维码/登录页面；
- [ ] 完成扫码；
- [ ] 停止并再次启动；
- [ ] 证明登录态是否保留；
- [ ] 记录 CPU、内存和磁盘占用。

### Task 03：验证源数据读取

- [ ] 定位数据目录；
- [ ] 判断数据库是否加密；
- [ ] 只读导出联系人、会话和文本消息样本；
- [ ] 导出一种媒体并计算哈希；
- [ ] 将样本脱敏后放入 `tests/fixtures/`；
- [ ] 记录失败原因和版本依赖。

### Task 04：实现归档数据库骨架

- [ ] 建立迁移脚本；
- [ ] 建立核心表和索引；
- [ ] 启用 WAL、外键和 FTS5；
- [ ] 编写重复导入测试；
- [ ] 编写跨账号隔离测试。

### Task 05：实现 Fixture Adapter 与导入引擎

- [ ] 用固定样本实现 Adapter；
- [ ] 完成联系人、会话、消息、媒体标准化；
- [ ] 完成批量事务和幂等 upsert；
- [ ] 完成 checkpoint 和回看窗口；
- [ ] 完成失败重试测试。

### Task 06：接入真实 Adapter

- [ ] 用 P0 确定的路线替换 Fixture Adapter；
- [ ] 保持归档层接口不变；
- [ ] 完成首次导入和第二次增量导入；
- [ ] 记录真实账号测试证据；
- [ ] 若 P0 路线改变，先更新本计划再继续。

### Task 07：提供最小 API/UI

- [ ] Dashboard；
- [ ] 账号状态；
- [ ] 手动同步按钮；
- [ ] 同步任务状态；
- [ ] 会话和消息查看；
- [ ] 文本搜索。

### Task 08：封装 Docker 部署

- [ ] 编写 Dockerfile 和 Compose；
- [ ] 显式声明 `/data`；
- [ ] 配置健康检查；
- [ ] 验证重启和迁移；
- [ ] 补充认证、HTTPS 和 Docker Socket 安全说明。

---

## 15. 开发纪律

1. 先验证数据链路，再写完整 UI。
2. 适配器隔离微信版本差异，归档模型不绑定源库表名。
3. 增量同步以不漏数据为第一目标，允许回看和重复扫描。
4. 所有同步步骤必须可重试、可恢复、可观察。
5. 微信源数据只读，Archive 数据追加式保存。
6. 不因为“服务器性能一般”而引入不必要的微服务。
7. 不把未验证的社区实现写成稳定依赖。
8. 不增加定时任务、消息发送或其他超出本计划的功能。
9. 每个阶段都以退出条件验收，不以“代码写完”验收。
10. 任何 Runtime、数据库解密或协议路线变化，都先更新 P0 报告和本计划。

---

## 16. 当前结论

项目可以按以下顺序进入开发：

```text
P0 运行时与数据读取可行性
  -> Phase 1 归档数据库和 Fixture
  -> Phase 2 手动同步引擎
  -> Phase 3 API
  -> Phase 4 只读 Web UI
  -> Phase 5 Docker 部署
  -> Phase 6 稳定化
```

当前唯一最高优先级是：

> 证明“目标环境中的微信数据能够被稳定、只读、可重复地读取并转化为归档记录”。

在这个结论得到证据之前，不应承诺扫码后自动真实同步、Linux Docker 跨版本数据库读取或全部媒体类型支持。离线导入和合成演示仅验证归档核心，不构成扫码同步验收。
